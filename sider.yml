linter:
  rubocop:
    config: 'lint_yml/.myrubocop.yml'

  eslint:
    root_dir: './'
    npm_install: false
    config: '.eslintrc'
    ext: 'js,jsx'

  stylelint:
    root_dir: 'src'
    glob: '**/*.{js}'
